'use client'
import { Menu, X, ArrowLeft } from 'lucide-react'
import { useState } from 'react'
const links = [['GTA VI','#hero'],['اخبار','#news'],['راهنماها','#features'],['نقشه','#features'],['درباره ما','#about']]
export function Header() {
  const [open, setOpen] = useState(false)
  return <header className="absolute inset-x-0 top-0 z-30"><div className="container-shell flex h-20 items-center justify-between"><a href="#hero" className="group flex items-center gap-3" aria-label="GTA فارسی"><span className="grid h-10 w-10 place-items-center rounded-full border border-primary/40 bg-primary/10 text-xs font-black text-primary shadow-[0_0_22px_rgba(240,74,154,.25)] transition group-hover:bg-primary group-hover:text-white">VI</span><span className="text-lg font-bold tracking-tight">GTA <span className="text-primary">فارسی</span></span></a><nav className="hidden items-center gap-8 text-sm text-muted md:flex">{links.map(([label, href], i) => <a key={label} href={href} className="group flex items-center gap-2 transition hover:text-foreground">{label}{i > 1 && <span className="rounded-full bg-secondary/15 px-1.5 py-0.5 text-[9px] text-secondary">به‌زودی</span>}</a>)}</nav><a href="#newsletter" className="hidden items-center gap-2 rounded-full border border-primary/40 px-4 py-2 text-sm font-semibold text-primary transition hover:bg-primary hover:text-white sm:flex">دریافت اخبار <ArrowLeft size={15}/></a><button type="button" onClick={() => setOpen(!open)} className="rounded-full border border-border p-2 md:hidden" aria-label={open ? 'بستن منو' : 'باز کردن منو'} aria-expanded={open}>{open ? <X size={20}/> : <Menu size={20}/>}</button></div>{open && <nav className="container-shell border-t border-border bg-[#08070d]/95 py-5 backdrop-blur-xl md:hidden">{links.map(([label, href]) => <a onClick={() => setOpen(false)} key={label} href={href} className="flex items-center justify-between border-b border-border/60 py-3 text-sm text-muted last:border-0">{label}<span className="text-xs text-secondary">به‌زودی</span></a>)}</nav>}</header>
}
